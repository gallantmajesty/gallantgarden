/**
 * Leaf-currency icon paths — single source of truth so the green (XP-earned)
 * and golden (premium) leaves can never be mixed up again.
 * Files: public/icons/green leaf.png (green) · public/icons/golden leaf.png (gold)
 * (filenames match the on-disk names; spaces are %20-encoded in URLs)
 */
export const GREEN_LEAF_ICON = '/icons/green%20leaf.png'
export const GOLD_LEAF_ICON = '/icons/golden%20leaf.png'
